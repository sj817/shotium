@CLAUDE.md

This file only points at `CLAUDE.md` in the same directory. Read that file
first: it is the single set of project instructions for every coding agent,
and it says where the skills are and how to expose them to tools that read
`.agents/skills/`.
