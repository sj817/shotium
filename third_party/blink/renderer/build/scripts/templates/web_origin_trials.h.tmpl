{% from 'templates/macros.tmpl' import license, source_files_for_generated_file %}
{{license()}}

{{source_files_for_generated_file(template_file, input_files)}}

#ifndef {{header_guard}}
#define {{header_guard}}

#include "third_party/blink/public/platform/web_common.h"

namespace blink {

class WebDocument;

class BLINK_EXPORT WebOriginTrials {
 public:
{% for feature in features %}
{% if feature.public and feature.in_origin_trial %}
  static bool Is{{feature.name}}Enabled(const WebDocument* web_document);
  // The v8::Local<v8::Context> overload that used to live here let embedders
  // check origin-trial status from a raw V8 context (e.g. from a
  // v8::FunctionCallbackInfo). There is no V8 build here, so that entry
  // point is gone; the WebDocument overload above is the only way in now.
{% endif %}
{% endfor %}

  WebOriginTrials() = delete;
};

}  // namespace blink

#endif  // {{header_guard}}
