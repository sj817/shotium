include_rules = [
    # This directory no longer has a media playback pipeline (see
    # html_media_element.h's file comment): the WebMediaPlayer, mojo
    # MediaPlayer/MediaPlayerHost/MediaPlayerObserver, RemotePlayback and
    # media_session includes that used to be allowlisted here have no
    # remaining caller.
]

specific_include_rules = {
    # Tests need to wait to check mojo messages are correctly sent.
    "html_media_element_test\\.cc": [
        "+base/run_loop.h",
    ],
    "media_fragment_uri_parser.cc": [
        "+base/no_destructor.h",
    ],
}
