// Copyright 2026 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Generated by scripts/gen-idl.ts unions from third_party/blink/renderer/core/dom/child_node.idl.
// Do not edit; edit the .idl and re-run the generator.

#ifndef THIRD_PARTY_BLINK_RENDERER_BINDINGS_CORE_V8_V8_UNION_STRING_TRUSTEDHTML_H_
#define THIRD_PARTY_BLINK_RENDERER_BINDINGS_CORE_V8_V8_UNION_STRING_TRUSTEDHTML_H_

#include <utility>

#include "base/check_op.h"
#include "third_party/blink/renderer/platform/bindings/union_base.h"
#include "third_party/blink/renderer/platform/heap/garbage_collected.h"
#include "third_party/blink/renderer/platform/heap/member.h"
#include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"

namespace blink {

class TrustedHTML;

// IDL union (TrustedHTML or DOMString).
class V8UnionStringOrTrustedHTML final : public bindings::UnionBase {
 public:
  // The type of the content value of this IDL union.
  enum class ContentType {
    kString,
    kTrustedHTML,
  };

  explicit V8UnionStringOrTrustedHTML(const String& value)
      : content_type_(ContentType::kString), member_string_(value) {}

  explicit V8UnionStringOrTrustedHTML(String&& value)
      : content_type_(ContentType::kString), member_string_(std::move(value)) {}

  explicit V8UnionStringOrTrustedHTML(TrustedHTML* value)
      : content_type_(ContentType::kTrustedHTML), member_trusted_html_(value) {
    DCHECK(value);
  }

  // Returns the type of the content value.
  ContentType GetContentType() const { return content_type_; }

  bool IsString() const { return content_type_ == ContentType::kString; }
  const String& GetAsString() const {
    DCHECK_EQ(content_type_, ContentType::kString);
    return member_string_;
  }
  void Set(const String& value) {
    Clear();
    member_string_ = value;
    content_type_ = ContentType::kString;
  }
  void Set(String&& value) {
    Clear();
    member_string_ = std::move(value);
    content_type_ = ContentType::kString;
  }

  bool IsTrustedHTML() const { return content_type_ == ContentType::kTrustedHTML; }
  TrustedHTML* GetAsTrustedHTML() const {
    DCHECK_EQ(content_type_, ContentType::kTrustedHTML);
    return member_trusted_html_.Get();
  }
  void Set(TrustedHTML* value) {
    DCHECK(value);
    Clear();
    member_trusted_html_ = value;
    content_type_ = ContentType::kTrustedHTML;
  }

  // Defined in the .cc: tracing a Member<T> needs T complete, and T's
  // header cannot be included here without a cycle.
  void Trace(Visitor* visitor) const override;

  static constexpr const char* UnionNameInIDL() { return "(DOMString or TrustedHTML)"; }

 private:
  void Clear() {
    member_string_ = String();
    member_trusted_html_.Clear();
  }

  ContentType content_type_;

  String member_string_;
  Member<TrustedHTML> member_trusted_html_;
};

}  // namespace blink

#endif  // THIRD_PARTY_BLINK_RENDERER_BINDINGS_CORE_V8_V8_UNION_STRING_TRUSTEDHTML_H_
