// Copyright 2026 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Generated by scripts/gen-idl.ts enums from third_party/blink/renderer/core/timing/preload_data.idl.
// Do not edit; edit the .idl and re-run the generator.

#ifndef THIRD_PARTY_BLINK_RENDERER_BINDINGS_CORE_V8_V8_CROSS_ORIGIN_MODE_H_
#define THIRD_PARTY_BLINK_RENDERER_BINDINGS_CORE_V8_V8_CROSS_ORIGIN_MODE_H_

#include <array>

#include "third_party/blink/renderer/platform/bindings/enumeration_base.h"
#include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"

namespace blink {

// IDL enumeration CrossOriginMode.
class V8CrossOriginMode final : public bindings::EnumerationBase {
 public:
  enum class Enum : enum_int_t {
    kNone,
    kAnonymous,
    kUseCredentials,
  };

  // std::array rather than a C array: indexing a raw array is
  // -Wunsafe-buffer-usage, which this tree builds with -Werror, and the index
  // below is a hard error in 590 translation units otherwise. std::array's
  // operator[] is a member function, so it is not caught by that warning and
  // is bounds-checked under libc++ hardening.
  static constexpr std::array<const char*, 3> kEnumStrings = {
      "none",
      "anonymous",
      "use-credentials",
  };

  // Not explicit: upstream's generated class is implicitly constructible from
  // its Enum, and call sites rely on that (`return V8FillMode::Enum::kBoth;`).
  constexpr V8CrossOriginMode(Enum value)
      : bindings::EnumerationBase(static_cast<enum_int_t>(value),
                                  kEnumStrings[static_cast<enum_int_t>(value)]) {}

  constexpr V8CrossOriginMode(const V8CrossOriginMode&) = default;
  constexpr V8CrossOriginMode(V8CrossOriginMode&&) = default;
  V8CrossOriginMode& operator=(const V8CrossOriginMode&) = default;
  V8CrossOriginMode& operator=(V8CrossOriginMode&&) = default;
  ~V8CrossOriginMode() = default;

  Enum AsEnum() const { return static_cast<Enum>(GetEnumValue()); }

 private:
  // Only the generated Create() produced an empty instance, to represent
  // "an exception was thrown while converting". Nothing here throws.
  constexpr V8CrossOriginMode() = default;
};

}  // namespace blink

#endif  // THIRD_PARTY_BLINK_RENDERER_BINDINGS_CORE_V8_V8_CROSS_ORIGIN_MODE_H_
